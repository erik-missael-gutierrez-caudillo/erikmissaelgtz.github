'use strict'
function peticion() {
  prompt("¿Cual es tu nombre(s)?");
 if(peticion <= peticion.length[2] || peticion === isNaN) {
    peticion;
 } else if (peticion >= peticion.length[3] && peticion === !isNaN){
    prompt("¿Cuales son tus apellidos?");
 } if(peticion <= peticion.length[2] || peticion === isNaN) {
    peticion;
 } else if (peticion >= peticion.length[3] && peticion === !isNaN){
    prompt("¿Cual es tu correo electrónico?");
 } if(peticion <= peticion.length[9]) {
    peticion;
 } else if (peticion >= peticion.length[10]){
    prompt("¿Cual es tu numero telefónico?");
 } 
}
 
peticion