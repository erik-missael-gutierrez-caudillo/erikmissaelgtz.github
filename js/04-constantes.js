'use strict'

// Constante
// Es una variable que no se puede cambiar 

var pais = "España"

const ciudad = "Madrid"

/* ---> ciudad = "Barcelona"  <-------- Esto no se puede hacer por que con constant no se puede cambiar el valor de la variable */

console.log(pais, ciudad);